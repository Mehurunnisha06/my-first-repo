name: Poster Upload Notification

on:
  push:  # Runs this workflow whenever there is a push to any branch
    branches:
      - ''  # all branches

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show success message
        run: echo "Poster Uploaded Successfully"
git add .github/workflows/poster-upload.yml
git push
